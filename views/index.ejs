app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, '/views'));

app.get('/', function(req, res) {
    const parametro = {
        titulo: 'Algorítimos Fundamentais'
    };
    res.render('index', parametro);
});
